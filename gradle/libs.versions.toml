[versions]
agp = "8.13.0"
plugin-api = "3.0.0-alpha"
plugin-packer = "1.0.0-alpha"
plugin-pusher = "1.0.0-alpha"

[libraries]
plugin-api = { group = "bin.mt.plugin", name = "api", version.ref = "plugin-api" }
plugin-packer = { group = "bin.mt.plugin", name = "packer", version.ref = "plugin-packer" }
plugin-pusher = { group = "bin.mt.plugin", name = "pusher", version.ref = "plugin-pusher" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }

